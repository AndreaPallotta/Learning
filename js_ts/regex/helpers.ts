const isBetweenLength = (min: number, max: number, inclusive: boolean) => {
    
}

const regexps = {
    number: /^\d+$/,
    email: /^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$/,
    between: (min: number = 10) => {}
}

